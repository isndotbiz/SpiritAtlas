package com.spiritatlas.core.ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

// Material 3 Base Colors
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ============================================================================
// SPIRITUAL PURPLES - Core mystical and spiritual colors
// ============================================================================
val CosmicViolet = Color(0xFF6B21A8)      // Deep cosmic purple
val MysticPurple = Color(0xFF7C3AED)      // Vibrant mystic purple
val AuroraPink = Color(0xFFEC4899)        // Aurora borealis pink
val SpiritualPurple = Color(0xFF8B5CF6)   // Main spiritual purple
val MysticViolet = Color(0xFF7C3AED)      // Deep mystic violet
val IntimacyPurple = Color(0xFFA855F7)    // Intimacy purple
val ChakraIndigo = Color(0xFF6366F1)      // Third eye chakra
val ChakraCrown = Color(0xFF8B5CF6)       // Crown chakra

// ============================================================================
// CELESTIAL BLUES - Night sky and cosmic themes
// ============================================================================
val NightSky = Color(0xFF1E1B4B)          // Deep night sky
val StardustBlue = Color(0xFF3B82F6)      // Bright stardust blue
val MoonlightSilver = Color(0xFFE2E8F0)   // Soft moonlight
val CosmicBlue = Color(0xFF3B82F6)        // Cosmic blue
val ChakraBlue = Color(0xFF3B82F6)        // Throat chakra
val AirCyan = Color(0xFF06B6D4)           // Air element cyan
val WaterTeal = Color(0xFF14B8A6)         // Water element teal
val DeepTeal = Color(0xFF008B8B)          // Deep teal

// ============================================================================
// CELESTIAL COLORS - Star, Cosmic, and Solar (10+ variations)
// ============================================================================

// Star Colors (metallic celestial tones)
val StarWhiteGold = Color(0xFFFFF9E6)     // White gold shimmer
val StarSilver = Color(0xFFC0C0C0)        // Silver starlight
val StarPlatinum = Color(0xFFE5E4E2)      // Platinum shine
val StarDiamond = Color(0xFFB9F2FF)       // Diamond sparkle
val StarCrystal = Color(0xFFF0F4F8)       // Crystal clear

// Cosmic Colors (deep space themes)
val CosmicDeepSpace = Color(0xFF0B0C1A)   // Deep space black
val CosmicNebulaPurple = Color(0xFF4C1D95) // Nebula purple
val CosmicNebulaPink = Color(0xFF9F1239)  // Nebula pink
val CosmicGalaxyBlue = Color(0xFF1E3A8A)  // Galaxy blue
val CosmicVoid = Color(0xFF1A1625)        // Void black
val CosmicStardust = Color(0xFF818CF8)    // Stardust purple
val CosmicAurora = Color(0xFF2DD4BF)      // Aurora green

// Solar Colors (sun and light themes)
val SolarGoldenHour = Color(0xFFFCD34D)   // Golden hour
val SolarSunrise = Color(0xFFFBBF24)      // Sunrise gold
val SolarSunset = Color(0xFFFF8C42)       // Sunset orange
val SolarNoon = Color(0xFFFEF3C7)         // High noon
val SolarDawn = Color(0xFFFFE4B5)         // Dawn light
val SolarDusk = Color(0xFFFF6B6B)         // Dusk crimson
val SolarFlare = Color(0xFFFFD700)        // Solar flare

// ============================================================================
// EARTH TONES - Grounding and sacred metals
// ============================================================================
val SacredGold = Color(0xFFD97706)        // Sacred temple gold
val TempleBronze = Color(0xFFB45309)      // Ancient bronze
val GroundingBrown = Color(0xFF78350F)    // Deep earth brown
val AuraGold = Color(0xFFFBBF24)          // Golden aura
val UnionGold = Color(0xFFF59E0B)         // Union gold

// ============================================================================
// ELEMENTAL COLORS - Fire, Water, Air, Earth (Expanded Collection)
// ============================================================================

// Fire Element Tones (8 variations)
val FireOrange = Color(0xFFF97316)        // Primary fire
val FireEmber = Color(0xFFDC2626)         // Deep ember red
val FireAmber = Color(0xFFFBBF24)         // Golden amber
val FireSunset = Color(0xFFFF6B35)        // Sunset orange
val FireCrimson = Color(0xFFB91C1C)       // Deep crimson
val FireCoal = Color(0xFF78350F)          // Burning coal
val FireFlame = Color(0xFFFB923C)         // Bright flame
val FireInferno = Color(0xFFEA580C)       // Intense inferno

// Water Element Tones (8 variations)
val WaterTealElement = Color(0xFF14B8A6)  // Primary water
val WaterTurquoise = Color(0xFF06B6D4)    // Tropical turquoise
val WaterAqua = Color(0xFF22D3EE)         // Clear aqua
val WaterDeepOcean = Color(0xFF0E7490)    // Deep ocean
val WaterSeafoam = Color(0xFF5EEAD4)      // Seafoam green
val WaterMist = Color(0xFFCCFBF1)         // Water mist
val WaterCoral = Color(0xFF0891B2)        // Coral reef
val WaterAbyss = Color(0xFF164E63)        // Ocean abyss

// Earth Element Tones (8 variations)
val EarthGreen = Color(0xFF22C55E)        // Primary earth
val EarthTerracotta = Color(0xFFD97706)   // Terracotta clay
val EarthSand = Color(0xFFFDE68A)         // Desert sand
val EarthForest = Color(0xFF15803D)       // Deep forest
val EarthMoss = Color(0xFF65A30D)         // Moss green
val EarthStone = Color(0xFF78716C)        // Stone gray
val EarthClay = Color(0xFFA16207)         // Rich clay
val EarthRoot = Color(0xFF52525B)         // Root brown

// Air Element Tones (8 variations)
val AirCyanElement = Color(0xFF06B6D4)    // Primary air
val AirSky = Color(0xFF0EA5E9)            // Sky blue
val AirCloud = Color(0xFFE0F2FE)          // Cloud white
val AirBreeze = Color(0xFF7DD3FC)         // Light breeze
val AirStorm = Color(0xFF0369A1)          // Storm blue
val AirMist = Color(0xFFBAE6FD)           // Morning mist
val AirFrost = Color(0xFFF0F9FF)          // Frost white
val AirThunder = Color(0xFF075985)        // Thunder gray

// ============================================================================
// CHAKRA SYSTEM - Seven primary energy centers
// ============================================================================
val ChakraRed = Color(0xFFEF4444)         // Root chakra (Muladhara)
val ChakraOrange = Color(0xFFF97316)      // Sacral chakra (Svadhisthana)
val ChakraYellow = Color(0xFFFDE047)      // Solar plexus (Manipura)
val ChakraGreen = Color(0xFF22C55E)       // Heart chakra (Anahata)
// ChakraBlue defined above               // Throat chakra (Vishuddha)
// ChakraIndigo defined above             // Third eye (Ajna)
// ChakraCrown defined above              // Crown chakra (Sahasrara)

// ============================================================================
// CHAKRA VARIATIONS - Light and Dark Tints (20% lighter/darker)
// ============================================================================

// Root Chakra Variations
val ChakraRedLight = Color(0xFFF87171)    // Root light (20% lighter)
val ChakraRedDark = Color(0xFFDC2626)     // Root dark (20% darker)
val ChakraRedPale = Color(0xFFFEE2E2)     // Root pale (for backgrounds)

// Sacral Chakra Variations
val ChakraOrangeLight = Color(0xFFFB923C) // Sacral light
val ChakraOrangeDark = Color(0xFFEA580C)  // Sacral dark
val ChakraOrangePale = Color(0xFFFFEDD5)  // Sacral pale

// Solar Plexus Variations
val ChakraYellowLight = Color(0xFFFEF08A) // Solar light
val ChakraYellowDark = Color(0xFFFACC15)  // Solar dark
val ChakraYellowPale = Color(0xFFFEF9C3)  // Solar pale

// Heart Chakra Variations
val ChakraGreenLight = Color(0xFF4ADE80)  // Heart light
val ChakraGreenDark = Color(0xFF16A34A)   // Heart dark
val ChakraGreenPale = Color(0xFFDCFCE7)   // Heart pale

// Throat Chakra Variations
val ChakraBlueLight = Color(0xFF60A5FA)   // Throat light
val ChakraBlueDark = Color(0xFF2563EB)    // Throat dark
val ChakraBluePale = Color(0xFFDBEAFE)    // Throat pale

// Third Eye Variations
val ChakraIndigoLight = Color(0xFF818CF8) // Third eye light
val ChakraIndigoDark = Color(0xFF4F46E5)  // Third eye dark
val ChakraIndigoPale = Color(0xFFE0E7FF)  // Third eye pale

// Crown Chakra Variations
val ChakraCrownLight = Color(0xFFA78BFA)  // Crown light
val ChakraCrownDark = Color(0xFF7C3AED)   // Crown dark
val ChakraCrownPale = Color(0xFFF3E8FF)   // Crown pale

// ============================================================================
// TANTRIC & RELATIONSHIP COLORS - Sacred intimacy
// ============================================================================
val TantricRose = Color(0xFFEC4899)       // Passionate rose
val SensualCoral = Color(0xFFF87171)      // Sensual coral
val CosmicOrange = Color(0xFFFF7F50)      // Cosmic orange

// ============================================================================
// LIGHT & DARK THEME FOUNDATION
// ============================================================================
val LightBackground = Color(0xFFFAFAFA)
val LightSurface = Color(0xFFFFFFFF)
val LightOnBackground = Color(0xFF1C1B1F)
val LightOnSurface = Color(0xFF1C1B1F)

val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkOnBackground = Color(0xFFE6E1E5)
val DarkOnSurface = Color(0xFFE6E1E5)

// ============================================================================
// ZODIAC ELEMENT PALETTES - Colors for each astrological element
// ============================================================================
object ZodiacElementColors {
  // Fire Signs: Aries, Leo, Sagittarius
  val FireSignColors = listOf(
    FireOrange,
    Color(0xFFDC2626),     // Crimson red
    Color(0xFFF59E0B),     // Amber
    Color(0xFFEA580C),     // Orange-red
    SacredGold
  )

  // Earth Signs: Taurus, Virgo, Capricorn
  val EarthSignColors = listOf(
    EarthGreen,
    GroundingBrown,
    TempleBronze,
    Color(0xFF059669),     // Emerald
    Color(0xFF84CC16)      // Lime
  )

  // Air Signs: Gemini, Libra, Aquarius
  val AirSignColors = listOf(
    AirCyan,
    StardustBlue,
    Color(0xFF0EA5E9),     // Sky blue
    Color(0xFF8B5CF6),     // Violet
    MoonlightSilver
  )

  // Water Signs: Cancer, Scorpio, Pisces
  val WaterSignColors = listOf(
    WaterTeal,
    DeepTeal,
    Color(0xFF3B82F6),     // Ocean blue
    CosmicViolet,
    Color(0xFF6366F1)      // Indigo
  )
}

// ============================================================================
// SPIRITUAL GRADIENTS - Beautiful gradient definitions
// ============================================================================
object SpiritualGradients {
  // Cosmic night sky gradient
  val cosmicNight: Brush = Brush.verticalGradient(
    colors = listOf(
      NightSky,
      CosmicViolet,
      MysticPurple
    )
  )

  // Aurora borealis gradient
  val auroraBorealis: Brush = Brush.linearGradient(
    colors = listOf(
      AuroraPink,
      MysticPurple,
      StardustBlue,
      WaterTeal
    )
  )

  // Golden hour gradient
  val goldenHour: Brush = Brush.horizontalGradient(
    colors = listOf(
      FireOrange,
      SacredGold,
      AuraGold,
      ChakraYellow
    )
  )

  // Chakra rainbow gradient (root to crown)
  val chakraRainbow: Brush = Brush.verticalGradient(
    colors = listOf(
      ChakraRed,
      ChakraOrange,
      ChakraYellow,
      ChakraGreen,
      ChakraBlue,
      ChakraIndigo,
      ChakraCrown
    )
  )

  // Mystic fog gradient
  val mysticFog: Brush = Brush.radialGradient(
    colors = listOf(
      MysticPurple.copy(alpha = 0.4f),
      CosmicViolet.copy(alpha = 0.2f),
      NightSky.copy(alpha = 0.1f)
    )
  )

  // Energy flow gradient
  val energyFlow: Brush = Brush.linearGradient(
    colors = listOf(
      SpiritualPurple,
      AuroraPink,
      SacredGold,
      WaterTeal
    )
  )

  // Tantric passion gradient
  val tantricPassion: Brush = Brush.linearGradient(
    colors = listOf(
      TantricRose,
      SensualCoral,
      IntimacyPurple
    )
  )

  // Sacred union gradient
  val sacredUnion: Brush = Brush.verticalGradient(
    colors = listOf(
      CosmicViolet,
      MysticPurple,
      UnionGold
    )
  )

  // Moonlight gradient
  val moonlight: Brush = Brush.verticalGradient(
    colors = listOf(
      NightSky,
      Color(0xFF334155),
      MoonlightSilver
    )
  )

  // Earth connection gradient
  val earthConnection: Brush = Brush.verticalGradient(
    colors = listOf(
      GroundingBrown,
      TempleBronze,
      SacredGold
    )
  )

  // ============================================================================
  // EXPANDED GRADIENT COLLECTION - Additional spiritual presets
  // ============================================================================

  // Aurora - Northern lights inspired gradient
  val aurora: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFF00FF87),      // Bright aurora green
      Color(0xFF60EFFF),      // Electric cyan
      AuroraPink,
      MysticPurple,
      ChakraIndigo
    )
  )

  // Cosmic Ocean - Deep space and water fusion
  val cosmicOcean: Brush = Brush.verticalGradient(
    colors = listOf(
      NightSky,
      Color(0xFF1E3A8A),      // Deep ocean blue
      Color(0xFF0891B2),      // Cyan-600
      WaterTeal,
      Color(0xFF2DD4BF)       // Teal-400
    )
  )

  // Desert Sunrise - Warm earth awakening
  val desertSunrise: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFF7C2D12),      // Dark earth brown
      TempleBronze,
      FireOrange,
      SacredGold,
      ChakraYellow,
      Color(0xFFFEF3C7)       // Soft yellow cream
    )
  )

  // Forest Mystical - Deep woodland magic
  val forestMystical: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFF14532D),      // Deep forest green
      Color(0xFF166534),      // Forest green-700
      EarthGreen,
      Color(0xFF4ADE80),      // Green-400
      Color(0xFF86EFAC),      // Green-300
      MoonlightSilver
    )
  )

  // Crystal Cave - Mineral shimmer and depth
  val crystalCave: Brush = Brush.radialGradient(
    colors = listOf(
      Color(0xFFE0E7FF),      // Indigo-100 (crystal center)
      Color(0xFFC7D2FE),      // Indigo-200
      ChakraIndigo,
      MysticPurple,
      Color(0xFF312E81),      // Indigo-900 (cave depth)
      NightSky
    )
  )

  // Volcanic Fire - Primal earth energy
  val volcanicFire: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFF450A0A),      // Red-950 (deep magma)
      Color(0xFF7F1D1D),      // Red-900
      ChakraRed,
      FireOrange,
      Color(0xFFFB923C),      // Orange-400
      SacredGold
    )
  )

  // Arctic Ice - Pure crystalline energy
  val arcticIce: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFFE0F2FE),      // Sky-100
      Color(0xFFBAE6FD),      // Sky-200
      Color(0xFF7DD3FC),      // Sky-300
      StardustBlue,
      AirCyan,
      Color(0xFF0C4A6E)       // Sky-900 (deep ice)
    )
  )

  // Tropical Paradise - Vibrant life force
  val tropicalParadise: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFFFEF08A),      // Yellow-200 (sunshine)
      Color(0xFFFDE047),      // Yellow-300
      Color(0xFFFBBF24),      // Yellow-400
      ChakraOrange,
      TantricRose,
      Color(0xFFDB2777),      // Pink-600
      Color(0xFF9333EA)       // Purple-600
    )
  )

  // Autumn Equinox - Balance of light and dark
  val autumnEquinox: Brush = Brush.horizontalGradient(
    colors = listOf(
      Color(0xFF7C2D12),      // Orange-950
      GroundingBrown,
      TempleBronze,
      Color(0xFFEA580C),      // Orange-600
      ChakraOrange,
      SacredGold,
      Color(0xFFFCD34D)       // Amber-300
    )
  )

  // Spring Bloom - Renewal and growth
  val springBloom: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFFFCE7F3),      // Pink-100 (soft dawn)
      Color(0xFFFBCFE8),      // Pink-200
      TantricRose,
      Color(0xFFF472B6),      // Pink-400
      Color(0xFFBBF7D0),      // Green-200 (new growth)
      EarthGreen,
      Color(0xFF22C55E)       // Green-500
    )
  )

  // ============================================================================
  // DARK MODE GRADIENT VARIANTS - Optimized for dark backgrounds
  // ============================================================================

  /**
   * Dark mode versions of gradients with enhanced luminance
   * All gradients tested for readability on dark backgrounds
   * Maintain spiritual essence while ensuring accessibility
   */

  // Aurora Dark - Enhanced brightness for visibility
  val auroraDark: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFF33FF99),      // Brighter aurora green
      Color(0xFF80F0FF),      // Lighter electric cyan
      Color(0xFFFF6BB5),      // Brighter aurora pink
      Color(0xFF9D7FF5),      // Lighter mystic purple
      Color(0xFF8B9CFF)       // Lighter chakra indigo
    )
  )

  // Cosmic Ocean Dark - Lightened for dark mode
  val cosmicOceanDark: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFF2D3A6B),      // Lighter night sky
      Color(0xFF2E5AAA),      // Lighter ocean blue
      Color(0xFF0EA5E9),      // Brighter cyan
      Color(0xFF2DD4BF),      // Bright teal
      Color(0xFF5EEAD4)       // Very light teal
    )
  )

  // Desert Sunrise Dark - Vibrant warm tones
  val desertSunriseDark: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFFB4531D),      // Lighter earth brown
      Color(0xFFE67E22),      // Vibrant bronze
      Color(0xFFFF9352),      // Bright fire orange
      Color(0xFFFFC857),      // Bright gold
      Color(0xFFFFEB99),      // Bright yellow
      Color(0xFFFFF8DC)       // Cream highlight
    )
  )

  // Forest Mystical Dark - Luminous greens
  val forestMysticalDark: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFF1F5D3D),      // Lighter forest green
      Color(0xFF22995D),      // Lighter forest green
      Color(0xFF4ADE80),      // Bright earth green
      Color(0xFF6EE7A7),      // Very bright green
      Color(0xFFA7F3D0),      // Light green
      Color(0xFFE0E7FF)       // Moonlight highlight
    )
  )

  // Crystal Cave Dark - Shimmering indigos
  val crystalCaveDark: Brush = Brush.radialGradient(
    colors = listOf(
      Color(0xFFF0F4FF),      // Very light center
      Color(0xFFDDD6FE),      // Light indigo
      Color(0xFF8B9CFF),      // Bright indigo
      Color(0xFF9D7FF5),      // Lighter mystic purple
      Color(0xFF5B4A8A),      // Medium indigo
      Color(0xFF2D3A6B)       // Lighter night sky
    )
  )

  // Volcanic Fire Dark - Bright flames
  val volcanicFireDark: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFF7F1D1D),      // Lighter deep magma
      Color(0xFFB91C1C),      // Lighter red
      Color(0xFFFF6B6B),      // Bright red
      Color(0xFFFF9352),      // Bright orange
      Color(0xFFFFC857),      // Bright gold
      Color(0xFFFFE082)       // Light gold
    )
  )

  // Arctic Ice Dark - Brilliant ice crystals
  val arcticIceDark: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFFF0F9FF),      // Very light ice
      Color(0xFFD0E8FF),      // Light sky blue
      Color(0xFF93DDFF),      // Bright sky
      Color(0xFF60EFFF),      // Electric cyan
      Color(0xFF22D3EE),      // Bright cyan
      Color(0xFF164E63)       // Deep ice accent
    )
  )

  // Tropical Paradise Dark - Vivid life energy
  val tropicalParadiseDark: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFFFFF59D),      // Bright sunshine
      Color(0xFFFFEB3B),      // Vivid yellow
      Color(0xFFFFC857),      // Bright gold
      Color(0xFFFF9352),      // Bright orange
      Color(0xFFFF6BB5),      // Bright pink
      Color(0xFFEC4899),      // Vibrant pink
      Color(0xFFA855F7)       // Bright purple
    )
  )

  // Autumn Equinox Dark - Warm harvest tones
  val autumnEquinoxDark: Brush = Brush.horizontalGradient(
    colors = listOf(
      Color(0xFFB4531D),      // Lighter earth brown
      Color(0xFFD97706),      // Sacred gold
      Color(0xFFE67E22),      // Vibrant bronze
      Color(0xFFFF9352),      // Bright orange
      Color(0xFFFFC857),      // Bright gold
      Color(0xFFFFE082),      // Light gold
      Color(0xFFFFF59D)       // Pale amber
    )
  )

  // Spring Bloom Dark - Fresh growth energy
  val springBloomDark: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFFFFF1F8),      // Very soft pink dawn
      Color(0xFFFFE4EC),      // Light pink
      Color(0xFFFF6BB5),      // Bright pink
      Color(0xFFFF91C7),      // Medium bright pink
      Color(0xFFD9F99D),      // Bright lime
      Color(0xFF4ADE80),      // Bright green
      Color(0xFF22C55E)       // Vibrant green
    )
  )

  // ============================================================================
  // NEW EXPANDED GRADIENTS - 15 Additional Spiritual Gradients
  // ============================================================================

  // 1. Complete Chakra Flow - All 7 chakras flowing upward
  val chakraFlow: Brush = Brush.verticalGradient(
    colors = listOf(
      ChakraRed,          // Root
      ChakraOrange,       // Sacral
      ChakraYellow,       // Solar Plexus
      ChakraGreen,        // Heart
      ChakraBlue,         // Throat
      ChakraIndigo,       // Third Eye
      ChakraCrown         // Crown
    )
  )

  // 2. Fire Element Journey - From ember to inferno
  val fireJourney: Brush = Brush.linearGradient(
    colors = listOf(
      FireCoal,
      FireEmber,
      FireCrimson,
      FireOrange,
      FireFlame,
      FireSunset,
      FireAmber
    )
  )

  // 3. Water Element Flow - Ocean depths to surface
  val waterFlow: Brush = Brush.verticalGradient(
    colors = listOf(
      WaterAbyss,
      WaterDeepOcean,
      WaterCoral,
      WaterTealElement,
      WaterAqua,
      WaterSeafoam,
      WaterMist
    )
  )

  // 4. Air Element Ascension - Ground to sky
  val airAscension: Brush = Brush.verticalGradient(
    colors = listOf(
      AirThunder,
      AirStorm,
      AirCyanElement,
      AirSky,
      AirBreeze,
      AirMist,
      AirFrost
    )
  )

  // 5. Earth Element Grounding - Deep roots to canopy
  val earthGrounding: Brush = Brush.verticalGradient(
    colors = listOf(
      EarthRoot,
      EarthStone,
      EarthClay,
      EarthTerracotta,
      EarthForest,
      EarthMoss,
      EarthGreen
    )
  )

  // 6. Cosmic Starfield - Deep space to starlight
  val cosmicStarfield: Brush = Brush.radialGradient(
    colors = listOf(
      StarCrystal,
      StarDiamond,
      StarPlatinum,
      CosmicStardust,
      CosmicGalaxyBlue,
      CosmicNebulaPurple,
      CosmicDeepSpace
    )
  )

  // 7. Solar Eclipse - Day meets night
  val solarEclipse: Brush = Brush.linearGradient(
    colors = listOf(
      SolarDawn,
      SolarGoldenHour,
      SolarSunrise,
      SolarFlare,
      SolarSunset,
      SolarDusk,
      NightSky
    )
  )

  // 8. Healing Energy - Soft therapeutic gradients
  val healingEnergy: Brush = Brush.radialGradient(
    colors = listOf(
      ChakraGreenPale,
      ChakraGreenLight,
      ChakraGreen,
      WaterSeafoam,
      AirMist,
      ChakraBluePale
    )
  )

  // 9. Meditation Serenity - Calm and peaceful
  val meditationSerenity: Brush = Brush.verticalGradient(
    colors = listOf(
      ChakraCrownPale,
      ChakraIndigoPale,
      ChakraBluePale,
      AirCloud,
      MoonlightSilver,
      StarCrystal
    )
  )

  // 10. Tantric Awakening - Passionate sacred energy
  val tantricAwakening: Brush = Brush.linearGradient(
    colors = listOf(
      CosmicViolet,
      IntimacyPurple,
      TantricRose,
      SensualCoral,
      ChakraOrange,
      UnionGold
    )
  )

  // 11. Divine Masculine - Powerful grounding energy
  val divineMasculine: Brush = Brush.verticalGradient(
    colors = listOf(
      EarthRoot,
      GroundingBrown,
      TempleBronze,
      FireCrimson,
      ChakraRed,
      FireOrange
    )
  )

  // 12. Divine Feminine - Nurturing flowing energy
  val divineFeminine: Brush = Brush.verticalGradient(
    colors = listOf(
      MoonlightSilver,
      StarPlatinum,
      AuroraPink,
      TantricRose,
      IntimacyPurple,
      MysticPurple
    )
  )

  // 13. Lunar Phases - Moon cycle journey
  val lunarPhases: Brush = Brush.horizontalGradient(
    colors = listOf(
      NightSky,
      CosmicDeepSpace,
      CosmicVoid,
      MoonlightSilver,
      StarWhiteGold,
      StarCrystal
    )
  )

  // 14. Elemental Balance - All four elements unified
  val elementalBalance: Brush = Brush.linearGradient(
    colors = listOf(
      FireOrange,       // Fire
      EarthGreen,       // Earth
      AirCyanElement,   // Air
      WaterTealElement  // Water
    )
  )

  // 15. Ascension Path - Spiritual awakening journey
  val ascensionPath: Brush = Brush.verticalGradient(
    colors = listOf(
      ChakraRed,           // Physical realm
      ChakraOrange,        // Emotional realm
      ChakraYellow,        // Mental realm
      ChakraGreen,         // Heart center
      ChakraBlue,          // Expression
      ChakraIndigo,        // Intuition
      ChakraCrown,         // Crown
      StarWhiteGold,       // Transcendence
      CosmicStardust       // Unity
    )
  )

  // ============================================================================
  // DARK MODE NEW GRADIENTS - Optimized for dark backgrounds
  // ============================================================================

  val chakraFlowDark: Brush = Brush.verticalGradient(
    colors = listOf(
      ChakraRedLight,
      ChakraOrangeLight,
      ChakraYellowLight,
      ChakraGreenLight,
      ChakraBlueLight,
      ChakraIndigoLight,
      ChakraCrownLight
    )
  )

  val fireJourneyDark: Brush = Brush.linearGradient(
    colors = listOf(
      Color(0xFFB4531D),
      Color(0xFFFF6B6B),
      Color(0xFFFF8C42),
      Color(0xFFFF9352),
      Color(0xFFFFC857),
      Color(0xFFFFD700),
      Color(0xFFFFF59D)
    )
  )

  val waterFlowDark: Brush = Brush.verticalGradient(
    colors = listOf(
      Color(0xFF164E63),
      Color(0xFF0E7490),
      Color(0xFF0891B2),
      Color(0xFF14B8A6),
      Color(0xFF22D3EE),
      Color(0xFF5EEAD4),
      Color(0xFFCCFBF1)
    )
  )

  val healingEnergyDark: Brush = Brush.radialGradient(
    colors = listOf(
      Color(0xFFDCFCE7),
      Color(0xFF86EFAC),
      Color(0xFF4ADE80),
      Color(0xFF5EEAD4),
      Color(0xFFBAE6FD),
      Color(0xFFDBEAFE)
    )
  )

  val cosmicStarfieldDark: Brush = Brush.radialGradient(
    colors = listOf(
      Color(0xFFFFFFF0),
      Color(0xFFD0E8FF),
      Color(0xFFC7D2FE),
      Color(0xFF93C5FD),
      Color(0xFF60A5FA),
      Color(0xFF3B82F6),
      Color(0xFF1E3A8A)
    )
  )
}

// ============================================================================
// GRADIENT COLLECTIONS - Organized gradient lists
// ============================================================================
val SpiritualGradient = listOf(SpiritualPurple, MysticViolet, CosmicBlue)
val ChakraGradient = listOf(
  ChakraRed,
  ChakraOrange,
  ChakraYellow,
  ChakraGreen,
  ChakraBlue,
  ChakraIndigo,
  ChakraCrown
)
val TantricGradient = listOf(TantricRose, SensualCoral, IntimacyPurple)
val CosmicGradient = listOf(NightSky, CosmicViolet, StardustBlue, MoonlightSilver)
val ElementalGradient = listOf(FireOrange, EarthGreen, AirCyan, WaterTeal)

// ============================================================================
// SPIRITUAL COLORS COLLECTION - Organized color mappings
// ============================================================================
object SpiritualColors {
  val ChakraColors = listOf(
    ChakraRed,    // Root (Muladhara)
    ChakraOrange, // Sacral (Svadhisthana)
    ChakraYellow, // Solar Plexus (Manipura)
    ChakraGreen,  // Heart (Anahata)
    ChakraBlue,   // Throat (Vishuddha)
    ChakraIndigo, // Third Eye (Ajna)
    ChakraCrown   // Crown (Sahasrara)
  )

  val TantricColors = mapOf(
    "Passionate" to TantricRose,
    "Sensual" to SensualCoral,
    "Sacred" to IntimacyPurple,
    "Divine" to SpiritualPurple,
    "Union" to UnionGold,
    "Mystical" to MysticViolet,
    "Cosmic" to CosmicBlue
  )

  val NumerologyColors = mapOf(
    1 to ChakraRed,
    2 to ChakraOrange,
    3 to ChakraYellow,
    4 to ChakraGreen,
    5 to ChakraBlue,
    6 to ChakraIndigo,
    7 to ChakraCrown,
    8 to AuraGold,
    9 to MysticViolet
  )

  val ElementColors = mapOf(
    "Fire" to FireOrange,
    "Earth" to EarthGreen,
    "Air" to AirCyan,
    "Water" to WaterTeal
  )

  val PlanetaryColors = mapOf(
    "Sun" to SacredGold,
    "Moon" to MoonlightSilver,
    "Mercury" to AirCyan,
    "Venus" to TantricRose,
    "Mars" to ChakraRed,
    "Jupiter" to SpiritualPurple,
    "Saturn" to NightSky,
    "Uranus" to StardustBlue,
    "Neptune" to WaterTeal,
    "Pluto" to CosmicViolet
  )

  val AyurvedicDoshaColors = mapOf(
    "Vata" to AirCyan,
    "Pitta" to FireOrange,
    "Kapha" to EarthGreen
  )
}

// ============================================================================
// SHADOW & GLOW COLORS - For elevation and effects
// ============================================================================
object ShadowColors {
  // Purple-tinted shadows for spiritual feel
  val LightShadow = CosmicViolet.copy(alpha = 0.08f)
  val MediumShadow = CosmicViolet.copy(alpha = 0.12f)
  val DarkShadow = CosmicViolet.copy(alpha = 0.16f)

  // Glow effects
  val PurpleGlow = MysticPurple.copy(alpha = 0.3f)
  val GoldGlow = SacredGold.copy(alpha = 0.3f)
  val BlueGlow = StardustBlue.copy(alpha = 0.3f)
  val PinkGlow = AuroraPink.copy(alpha = 0.3f)
}

// ============================================================================
// SEMANTIC COLORS - Success, Error, Warning, Info
// ============================================================================
object SemanticColors {
  val Success = ChakraGreen
  val Error = ChakraRed
  val Warning = SacredGold
  val Info = StardustBlue

  val SuccessContainer = ChakraGreen.copy(alpha = 0.12f)
  val ErrorContainer = ChakraRed.copy(alpha = 0.12f)
  val WarningContainer = SacredGold.copy(alpha = 0.12f)
  val InfoContainer = StardustBlue.copy(alpha = 0.12f)
}

// ============================================================================
// OVERLAY COLORS - For scrim, dim, and overlay effects
// ============================================================================
object OverlayColors {
  val Scrim = Color(0x80000000)  // 50% black
  val DimLight = Color(0x26000000)  // 15% black
  val DimMedium = Color(0x4D000000)  // 30% black
  val DimHeavy = Color(0x99000000)  // 60% black

  val MysticOverlay = CosmicViolet.copy(alpha = 0.15f)
  val GoldenOverlay = SacredGold.copy(alpha = 0.15f)
  val CosmicOverlay = NightSky.copy(alpha = 0.7f)
}
