<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SpiritAtlasApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.SpiritAtlas"
        android:supportsRtl="true">
        
        <!-- Disable default WorkManager initialization since we use on-demand -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SpiritAtlas"
            android:launchMode="singleTop">

            <!-- Main launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom deep links (spiritualatlas://*) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="spiritualatlas" />
                <data android:host="" />
                <data android:pathPrefix="/home" />
                <data android:pathPrefix="/profile" />
                <data android:pathPrefix="/profiles" />
                <data android:pathPrefix="/compatibility" />
                <data android:pathPrefix="/comparison" />
                <data android:pathPrefix="/consent" />
                <data android:pathPrefix="/enrichment" />
            </intent-filter>

            <!-- App Links (https://spiritualatlas.app/*) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="spiritualatlas.app" />
                <data android:pathPrefix="/home" />
                <data android:pathPrefix="/profile" />
                <data android:pathPrefix="/profiles" />
                <data android:pathPrefix="/compatibility" />
                <data android:pathPrefix="/comparison" />
                <data android:pathPrefix="/consent" />
                <data android:pathPrefix="/enrichment" />
            </intent-filter>
        </activity>
        
    </application>

</manifest>
